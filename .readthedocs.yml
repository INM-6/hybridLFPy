version: 2

sphinx:
  configuration: docs/conf.py

formats: all

build:
  image: latest

conda:
  environment: docs/environment.yml

python:
    version: 3
    install:
        - method: pip
          path: .
